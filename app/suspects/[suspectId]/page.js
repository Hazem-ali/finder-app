import React from 'react';

const SuspectDetail = (props) => {
    return (
        <div>
            heeloo from detal {props.params.suspectId} 
        </div>
    );
}

export default SuspectDetail;
